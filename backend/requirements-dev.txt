# Install production dependencies first
-r requirements.txt

# --- Testing ---
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
coverage>=7.10.7

# --- Code quality (keep in sync with .pre-commit-config.yaml) ---
ruff>=0.14.0
mypy>=1.18.2
bandit>=1.8.6
pre-commit>=4.3.0

# --- Type stubs / plugins ---
sqlalchemy[mypy]>=2.0.52
types-redis>=4.6.0
