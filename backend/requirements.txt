# --- Web framework ---
fastapi[standard]==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.12.0
python-dotenv==1.2.3

# --- Database ---
# SQLite is the zero-setup default; PostgreSQL is optional (via asyncpg).
SQLAlchemy==2.0.52
greenlet>=3.0.0  # required for SQLAlchemy asyncio support
alembic==1.19.2
aiosqlite>=0.20.0
asyncpg>=0.29.0  # only needed for PostgreSQL

# --- Redis (used to inspect monitored queues) ---
redis==8.1.0
